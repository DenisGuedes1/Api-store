npm run typeorm migration:generate ./src/migrations/InitialMigration -- -d ./src/data-source.ts
rodar migration
npm run typeorm migration:run -- -d ./src/data-source

    // const addresRepository = AppDataSource.getRepository(Adresses);
    // addresRepository
    //     .findOne({
    //         where: { id: idAddres },
    //     })
    //     .then((findAddress) => {
    //         if (findAddress) {
    //             const updatedAddress = addresRepository.merge(
    //                 findAddress,
    //                 dataAddres
    //             );
    //             addresRepository.save(updatedAddress);
    //             const newUserAddres = createdAddreSchema.parse(dataAddres);
    //             return newUserAddres;
    //         } else {
    //             throw new AppError("Address not found", 404);
    //         }
    //     });